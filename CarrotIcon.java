import greenfoot.*;

/**
 * A carrot icon that shows when a rabbit has a carrot to throw
 * 
 * @author Daniel Bark  
 * @version 1.0
 */
public class CarrotIcon extends ProjectileIcon
{
    /**
     * Constructs a Carroticon
     */
    CarrotIcon(){
        getImage().scale(40, 40);
    }
}
